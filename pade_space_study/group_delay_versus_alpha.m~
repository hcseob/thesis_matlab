clear all; close all; run('~/thesis/matlab/thesis.m'); addpath('./lib');

delay = 1;
alpha = 1;
pade1 = pade_sys(1, delay, alpha);

[gain, phase, w] = bode(pade1);
phase = squeeze(phase)*pi/180;

figure;
semilogx(w/2/pi, phase*180/pi);

%%
